{
    "default_server_config": {
        "m.homeserver": {
            "base_url": "https://matrix.${MATRIX_SERVER_NAME}",
            "server_name": "${MATRIX_SERVER_NAME}"
        }
    },
    "brand": "Element",
    "disable_guests": true,
    "disable_3pid_login": true,
    "roomDirectory": {
        "servers": [
            "${MATRIX_SERVER_NAME}"
        ]
    },
    "default_theme": "dark"
}
